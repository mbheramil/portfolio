{
  "name": "mbheramil-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init":"wrangler d1 execute mbheramil --file=./schema.sql --remote",
    "db:local":"wrangler d1 execute mbheramil --file=./schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
